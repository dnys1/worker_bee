sdk:
  - stable
  - dev

stages:
  - analyze_and_format:
      - group:
          - format
          - analyze: --fatal-infos .
  - unit_test:
      - test:
      - test: -p chrome
      - test: -p firefox
  - unit_test_safari:
      - test: -p safari
        os:
          - macos
